spring:
  datasource:
    url: jdbc:h2:mem:scoring;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  flyway:
    enabled: true
  rabbitmq:
    dynamic: false
    listener:
      simple:
        auto-startup: false
      direct:
        auto-startup: false

app:
  scoring:
    demo-enabled: true
  relations:
    base-url: http://127.0.0.1:1
